# 2026-04-01 主车最小运行时 HIL 留证

## 状态

- 当前状态: 未执行, 待设备连接后执行
- 执行限制: 本轮仅预留步骤与留证路径, 不运行 `mpy-cli`, 不连接设备, 不记录伪结果
- 留证路径: `tests/hil/2026-04-01-master-minimal-runtime.md`
- 附件预留路径: `tests/hil/artifacts/2026-04-01-master-minimal-runtime/`

## 固定动作链

1. 设备发现: `mpy-cli list`
2. 上传规划: `mpy-cli plan`
3. 上传并运行: `mpy-cli deploy --port <master-port>`
4. 最小 smoke: `python3 tools/run_stage2_smoke.py --port <master-port> --source-dir src/master`
5. 最小查询或人工观测主循环
6. 记录观测项并形成结论

## 需要记录的观测项

- 设备端口或设备标识
- 本次上传和运行命令
- `mpy-cli plan` 输出中的目标路径与上传计划
- smoke 或最小查询的原始输出
- 是否进入主循环, 需要用可复核现象证明, 不能只凭串口有输出判断
- 最小诊断面是否仍可用
- 失败时的现象、时间点和可能线索

## 最小内存留证项

- 导入后最小可用内存
- 核心初始化后最小可用内存
- 功能初始化后最小可用内存
- 空闲运行时最小可用内存
- 低内存下最小诊断面是否仍可用
- 本轮保留或新增常驻项清单
- 每个常驻项的唯一归属、出现阶段与保留原因

## 执行结果

- 结果: 未执行, 待设备连接后执行
- 主机侧回归不能替代本页板端验证
