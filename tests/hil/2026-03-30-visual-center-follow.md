# 2026-03-30 辅车色标二维回中 HIL 留证

## 状态

- 当前状态: 待继续
- 原因: 主机侧与部署计划已通过, 但当前未探测到可用 MicroPython 设备, 配置端口 `/dev/cu.usbmodem11101` 也不存在, 暂停在设备连接阶段

## 目标

- 验证主车可持续接收双路视觉中的辅车色标位置
- 验证主车只根据二维偏差向辅车下发回中控制, 不进入主车主动运动
- 验证色标进入死区后辅车保持不动
- 验证色标丢失后辅车立即停住
- 验证辅车可回传最小状态, 至少包含当前状态、是否跟随中与最后一次控制序号

## 前置条件

- 已烧录当前阶段 `master` 与 `assistant` 固件
- 双路视觉输入已按统一语义输出 `err_x/err_y`, 且主线目标为辅车色标
- 主车与辅车串口连接正常, 可收发当前阶段最小协议
- 主辅车电机与编码器映射按 `src/legacy/hardware/` 中的正式硬件层实现保持一致
- 测试场地允许人工移动主车或移动目标, 但本阶段不要求主车主动运动
- 已知本阶段不验收灰度链路、不验收角度控制、不验收主车主动运动

## 上板步骤

1. 上电并启动主车与辅车当前阶段最小运行时
2. 记录本次测试版本、串口、视觉接入口与测试时间
3. 确认双路视觉都在持续输出辅车色标位置, 且主车能稳定收到最新结果
4. 让色标处于画面中心外, 人工缓慢移动主车或目标, 观察辅车是否按 `dx/dy` 方向执行二维回中
5. 继续人工调整, 让色标进入设定死区, 观察辅车是否保持不动
6. 遮挡或移除色标, 观察辅车是否立即停住
7. 在以上过程中记录辅车最小状态回传, 关注 `state_label`、`follow_active`、`last_seq`
8. 如双路视觉同时可用, 重复一次中心外到回中的过程, 确认主线现象稳定

## 预期结果

- 主车与辅车均可正常启动
- 主车可持续消费双路视觉中的辅车色标位置
- 主车只向辅车下发二维回中控制, 不出现主车主动运动行为
- 色标偏离中心时, 辅车会执行二维回中
- 色标进入死区后, 辅车保持不动
- 色标丢失后, 辅车立即停住
- 辅车状态回传至少可见 `state_label`、`follow_active`、`last_seq`
- 本次留证不包含灰度链路、不包含角度控制、不包含主车主动运动结论

## 记录项

- 测试版本:
- 测试时间:
- 主车端口:
- 辅车端口:
- 双路视觉接入说明:
- 回中现象:
- 死区保持现象:
- 色标丢失停住现象:
- 辅车最小状态回传样例:
- 主机侧验证: `python3 -m pytest tests/unit tests/contract -q` -> `93 passed`
- 部署计划: `mpy-cli plan --mode incremental --no-interactive --yes` -> `upload 26, delete 0`
- 设备发现: `mpy-cli list` -> 未探测到可用的 MicroPython 设备
- 连接验证: `ls /dev/cu.usbmodem11101` -> `No such file or directory`
- smoke 结果: `python3 tools/run_stage2_smoke.py --port /dev/cu.usbmodem11101` -> `deploy_failed`, 原因是 `mpremote` 无法访问该端口
- 备注: 已将 `.mpy-cli.toml` 的 `source_dir` 修正为 `src/master`, 当前阻塞点只剩设备端口/占用状态

## 失败时最小排查项

- 确认双路视觉是否真的在输出辅车色标, 且 `valid` 状态符合现场现象
- 确认双路视觉的 `err_x/err_y` 语义是否一致, 没有方向相反的问题
- 确认主车是否仍只输出当前阶段最小二维控制, 没有混入额外字段或旧链路
- 确认死区内是否仍在重复下发有效移动控制
- 确认色标丢失时是否已转为无效目标并停止下发移动
- 确认辅车是否正常回传最小状态, `last_seq` 是否随新控制更新

## 结论

- deploy_failed: 设备端口不存在或当前不可访问, 待用户连接设备或提供正确端口后继续
