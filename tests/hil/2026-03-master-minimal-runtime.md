# 2026-03 主车最小运行时 HIL 留证

## 状态

- 当前状态: 待执行
- 原因: 当前已完成主机侧验证, 但板端单路跟随联调尚未开始

## 目标

- 验证主车最小运行时可在板端启动
- 验证 `UART6` 单路视觉持续回传链路可工作
- 验证 `UART8` 仅作为预留口存在, 不进入当前验收主线
- 验证主车能把视觉误差映射为 `follow=1,...` 控制报文

## 预期步骤

1. 烧录当前 `master` 最小运行时到设备
2. 启动主车主程序
3. 记录当前测试版本与串口
4. 通过 `UART6` 持续输入 `vision=1,camera_id=cam_a,seq=...,valid=1,target=follower,err_x=...,err_y=...`
5. 观察主车是否输出 `follow=1,seq=...,valid=1,dx=...,dy=...,d_angle=...`
6. 再输入 `valid=0` 报文, 观察主车是否输出无目标保持报文

## 预期结果

- 主车可启动
- `UART6` 输入可被稳定消费
- 有目标时输出 `valid=1` 的跟随报文
- 无目标时输出 `valid=0` 的保持报文
- 当前验收路径不依赖 `UART8`

## 实测输出

- 版本:
- 端口:
- 视觉输入:
- 主车输出:
- 人工观察:
- 主机侧验证: `python3 -m pytest tests/unit tests/contract -q` -> `32 passed`

## 结论

- hil_pending
