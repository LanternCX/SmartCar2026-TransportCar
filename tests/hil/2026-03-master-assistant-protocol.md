# 2026-03 主辅最小运动协议 HIL 留证

## 状态

- 当前状态: 待执行
- 原因: 用户明确要求当前先不进行设备验证

## 目标

- 验证主车能正确下发最小运动命令
- 验证辅车能正确解析并执行最小运动命令
- 验证辅车最小状态回包与主车协议预期一致

## 预期步骤

1. 启动主车与辅车最小运行时
2. 由主车发送 `MOVE` / `HOLD`
3. 观察辅车回包 `ACK` / `BUSY` / `DONE` / `STATE`
4. 记录失败归因
5. 记录主机侧对应契约版本

## 预期结果

- `MOVE` 文本格式稳定
- 辅车能正确解析数值字段
- `HOLD` 能被识别并停止执行链
- `STATE` 回包至少包含 `armed` / `busy` / `last_cmd` / `odom` / `heading`

## 实测输出

- 版本:
- 主车发送:
- 辅车回包:
- 协议对齐判断:
- 失败归因:

## 结论

- hil_pending
