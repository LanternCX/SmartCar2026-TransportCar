[project]
name = "smartcar"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
test = [
    "pytest>=9.0.2",
]
type = [
    "pyright>=1.1.408",
]
board = [
    "mpy-cli",
    "mpremote==1.27.0",
    "mpy-cross==1.20.0",
]

[tool.uv]
package = false

[tool.uv.sources]
mpy-cli = { path = "../mpy-cli-dev", editable = true }
