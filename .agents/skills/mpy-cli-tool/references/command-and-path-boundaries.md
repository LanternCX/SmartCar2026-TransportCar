# mpy-cli 命令与路径边界入口

- 正文唯一落点：`references/mpy-cli-manual.md`
- 想先看命令总览、参数说明与无交互用法时，先读：`references/mpy-cli-manual.md`
- 想确认 `source_dir`、`device_upload_dir`、`.mpyignore` 与上传范围边界时，回手册 `## CLI 参数总览` 中的 `mpy-cli config`、`mpy-cli upload`、`mpy-cli run`、`mpy-cli delete`、`mpy-cli tree` 章节逐项核对
- 部署前默认先按正文建议执行 `mpy-cli list` 与 `mpy-cli plan`
