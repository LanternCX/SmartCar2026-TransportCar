# 2026-03-16-5
## Date
2026-03-16

## Category
refactor

## Title
继续拆分 diagnostics facade 并收紧只读 owner 聚合边界

## Background / Issue
`src/services/runtime/diagnostics_facade.py` 虽然已经被压到 300 非注释代码行以内, 但健康, tick, 运动, 视觉和格式化细节仍然堆在单文件里。当前计划的 Task 3 要求继续拆分内部职责, 保留 facade 公开入口, 并确保 diagnostics 仍然只是只读聚合 owner 状态, 不缓存第二份运行时状态。

## Change Unit
继续拆分 diagnostics facade 内部职责，并保持只读 owner 聚合边界。

## Actions / Outcome
- Approach 1: 只依赖现有 300 行 gate, 不再继续拆分 -> 门禁会通过, 但 facade 仍承担过多内部细节, 不符合 Task 3 目标
- Approach 2: 直接把字段常量和各类 snapshot builder 下沉到 `diag_format.py`、`diag_health.py`、`diag_motion.py`、`diag_vision.py`, facade 仅保留 owner 读取辅助和公开转发 -> `python3 -m pytest tests/unit/services/test_transport_car_diag_snapshots.py tests/contract/services/commands/test_diag_queries.py -q` 与 `python3 -m pytest tests/unit/test_src_runtime_file_sizes.py -q` 通过, 并新增结构测试锁定子模块存在
- Final approach: 在拆分后补回 `HEALTH_SNAPSHOT_FIELDS` 等兼容导出, 再补跑 `python3 -m pytest tests/unit/services/test_transport_car_logging.py -q` -> 保持 `services.diagnostics` 现有导入路径可用, 同时验证 `build_log_snapshot()` 未回归

## Lessons / Refinements
- 行数达标不等于职责健康; 对 facade 类模块, 还需要用结构测试锁定内部子模块拆分, 避免薄壳回弹成单文件 God module
- diagnostics 子模块可以按职责拆分实现细节, 但 facade 自身必须继续只读转发 owner 当前状态, 不应引入缓存镜像或 import-time 装配

## Related Commits
- 361124f refactor(services): split transport runtime into lazy packages

## Integration Summary
该变更在保持公开 facade 稳定的同时，将内部快照与格式化细节进一步按职责拆散。
