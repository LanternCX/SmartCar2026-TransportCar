# 双车协同搬运路线重规划文档改造实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 来执行本计划。步骤使用 checkbox (`- [ ]`) 语法跟踪。

**Goal:** 在不改变现有开发文档架构的前提下，重写 `strategy / tasks / control / vision` 四份文档，使其统一表达新的双车协同搬运路线与项目演进方向。

**Architecture:** 继续沿用当前 `docs/developer/` 下的四份正式开发文档分工，不新增新的文档层级。先改总方向文档，再改演进路线文档，再补控制与视觉职责，最后做一次跨文档一致性自检，确保“总路线、近期验证重点、关键能力、阶段视觉职责”四组信息相互对齐。

**Tech Stack:** Markdown 文档、现有 `docs/developer/*` 开发文档体系、`docs/superpowers/specs/*` 审核文档

**Git Note:** 本轮先产出 plan 供审核，不包含自动提交步骤。

---

## 范围确认

- 只修改项目开发文档，不改代码。
- 不新增新的开发文档结构，沿用 `strategy / tasks / control / vision`。
- 不引入新的硬件猜测、引脚定义、通信频率或协议字段。
- `0 / 90 / -90 / 180` 四类角度关系只固化为路线分支入口，不扩到动作级策略。
- “近期先做速度前馈验证”要写进演进路线，但不能替代总路线本身。

## 文件边界

- Modify: `docs/developer/strategy.md`
- Modify: `docs/developer/tasks.md`
- Modify: `docs/developer/control.md`
- Modify: `docs/developer/vision.md`
- Reference: `docs/superpowers/specs/2026-04-14-dual-car-route-evolution-docs-design.md`

## 任务拆分

### Task 1: 重写 `strategy.md` 的项目总方向

**Files:**
- Modify: `docs/developer/strategy.md`

- [ ] 重新整理“总目标”“方案路径”“搬运策略”“核心原则”几个章节，让文档先表达项目最终路线，而不是沿用旧的单一路线口径。
- [ ] 写清“寻找态状态机集 + 搬运态状态机集”是新的总结构。
- [ ] 写清主车和辅车都参与运动，但主车负责统一决策、搜索、切换和协同编排。
- [ ] 写清搬运至少包含“前后推”“左右推”两种构型，并把左右推描述为横向协同时的专门构型。
- [ ] 写清高频通信和速度前馈是整条路线里的关键能力。
- [ ] 删除或改写会把路线继续理解成“整体横向搬运、不重建阵型、主车不主动参与运动”的表述。

### Task 2: 重写 `tasks.md` 的项目演进路线

**Files:**
- Modify: `docs/developer/tasks.md`

- [ ] 保留现有阶段化任务文档的写法，不额外发明新的层级或新文档入口。
- [ ] 重写阶段目标，让前面阶段先承担“方向对齐、关键能力验证”的职责。
- [ ] 把“主车到辅车高频通信 + 速度前馈 + 跟随可行性验证”放到靠前阶段，写成近期优先任务。
- [ ] 把寻找态最小闭环、搬运态最小闭环、完整切换与角度分类、连续搬运与赛前封板，按演进顺序放进后续阶段。
- [ ] 明确四类角度关系是后续阶段必须接入的正式分支入口，但本轮任务只要求固化分支，不要求动作细化。
- [ ] 复核任务表述，确保“近期验证重点”没有盖过“总体项目路线”。

### Task 3: 重写 `control.md` 的控制侧判断依据

**Files:**
- Modify: `docs/developer/control.md`

- [ ] 收紧文档重点，让它回答“为什么这条路线需要高频通信和速度前馈”。
- [ ] 写明纯视觉跟随响应不够快，双车协同闭环偏软，这是引入速度前馈的直接原因。
- [ ] 写明主车向辅车转发运动意图，是为了增强双车跟随硬度，而不是单纯增加一条附属优化链路。
- [ ] 写明前馈策略按状态切换：寻找阶段和搬运阶段虽然都要用前馈，但服务对象和视觉输入不同。
- [ ] 删除或改写会把控制链继续理解成单一旧跟随口径的表述。

### Task 4: 重写 `vision.md` 的阶段视觉职责

**Files:**
- Modify: `docs/developer/vision.md`

- [ ] 把视觉职责从“单套统一视觉状态机”口径，改成“不同阶段服务不同任务”的口径。
- [ ] 写明寻找阶段由主车负责找下一个目标，辅车通过识别主车标记完成跟随。
- [ ] 写明搬运阶段视觉重点转为搬运物体和环境信息，为物体保持、构型切换和避障提供输入。
- [ ] 写明不同阶段使用不同视觉输入，不把整程视觉任务混写成同一种观测。
- [ ] 保持表述停留在职责层，不引入未确认的检测实现细节。

### Task 5: 做跨文档一致性自检

**Files:**
- Modify: `docs/developer/strategy.md`
- Modify: `docs/developer/tasks.md`
- Modify: `docs/developer/control.md`
- Modify: `docs/developer/vision.md`

- [ ] 逐份检查四份文档，确认都明确表达了“寻找态状态机集 + 搬运态状态机集”的总结构。
- [ ] 检查四份文档中对主车和辅车职责的描述是否一致，避免一处写主车主动参与，另一处仍写主车只做被动决策。
- [ ] 检查四份文档中对高频通信、速度前馈、阶段视觉输入的定位是否一致。
- [ ] 检查是否还残留“横向搬运固定主线”“不重建阵型”“单套状态机”等会误导后续实现的旧口径。
- [ ] 检查全文是否存在历史性口吻、占位词或动作级过度承诺，并在提交审核前收口。

## 验证方式

- 通过逐文件通读确认四份文档的分工和关键共识项一致。
- 通过关键词复查确认旧口径已被替换或删除。
- 通过人工审核确认“总路线”和“近期验证重点”的上下关系清楚可读。

## 自检

- [ ] 计划已经覆盖四份开发文档的修改顺序，没有遗漏需要同步的正式入口文档。
- [ ] 计划没有引入新的文档结构，也没有要求去写动作级实现细节。
- [ ] 计划已经明确本轮是文档改造，不包含代码开发、TDD 或自动提交。
