# 坐标语义

- 车体系 `y+` 表示向前。
- 车体系 `x+` 表示向右。
- 车体系 `w+` 表示顺时针旋转, 也就是右转。
- 涉及“前进 / 右移”动作时, 优先按上述车体系语义映射到 `set_relative_translation_target(dx, dy)`。
- 涉及“右转 / 左转”动作时, 右转使用正角度, 左转使用负角度。
- 若文档、脚本名或现场表现与该语义冲突, 先以现场确认语义为准, 再修正文字或测试说明。
