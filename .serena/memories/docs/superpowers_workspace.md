# docs/superpowers 工作区约定

- `docs/superpowers/` 是本仓库的 Spec / Plan 工作区。
- 该目录被 `.gitignore` 忽略是预期行为。
- 即使被忽略，用户明确要求写 Spec / Plan 时仍应写入 `docs/superpowers/` 下。
- 这些文件默认不进入 commit。
- 不要为了提交 Spec / Plan 将其迁移到 `docs/developer/`。
- 不要为了让 `docs/superpowers/` 进入版本管理修改 `.gitignore`。
