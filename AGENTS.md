## Info
注：非必要不查看
1. OpenART 视觉仓库: ../SmartCar2026-Vision
2. 主机端侧手柄控制上位机: ../SmartCar2026-Controller

## Rules
1. 对于任何硬件高度相关的开发，如果我没有给出具体的引脚号等硬件相关信息，请查找确认后你向我询问，不要进行自己的猜测。
2. 仓库新构建的所有文档和代码注释都使用中文，包括 Skill 以及 Plan。
3. 不要写不必要的兜底代码，除非这个地方真的值得写入。
4. 这个仓库因为竞赛需求，所有的重构都不需要保留兼容层。如果你觉得 TDD 的奖励会错使 Agent 更倾向于保留兼容层，请你先删除有关测试再进行重构。
5. 编码时应该主动根据规则维护注释，编码结束之后应该主动维护文档。
6. 有关参数的测试都不要写死，对于有明确依据的参数，可以加上参数范围或者枚举值进行测试，其余保持动态引入。
7. 由于我对当前项目的代码足够熟悉，使用 Superpowers 时:
   1. 除非我提及，否则不要撰写 Spec / Plan，而是直接进行修改
   2. 允许在我对当前改动描述不够明确的时候，向我提出澄清问题
   3. 允许在你认为当前的改动不适合当前方向的时候拒绝我提出的改动并给出理由
   4. 在需要撰写的 Spec / Plan 的场景，不要进行分段呈现，在对齐需求之后就可以直接撰写
   5. 不要向我推荐 Web View
   6. 如果撰写了 Spec / Plan，则默认使用 Subangent-Driven 进行开发